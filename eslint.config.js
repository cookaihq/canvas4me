/**
 * ESLint flat config(ESLint 9+)— 项目预留入口。
 *
 * 当前默认空配置:本仓库未在 devDependencies 中预装 eslint。如需启用,
 * 自行 `npm i -D eslint @eslint/js eslint-plugin-react eslint-plugin-react-hooks`
 * 后在此处补充规则。
 */
export default []
