// 分组节点共享常量。padding 单一来源（spec §6.1 铁律 3：一处定义）。
export const GROUP_PADDING = 24
export const GROUP_DEFAULT_SIZE = { width: 240, height: 160 }
// 6 色浅色板（与 NoteNode 共用同一组，spec §5.2）
export const GROUP_COLOR_PRESETS = ['#fffbe6', '#f6ffed', '#e6f7ff', '#fff0f6', '#f9f0ff', '#fff7e6']
export const GROUP_DEFAULT_COLOR = '#e6f7ff'
