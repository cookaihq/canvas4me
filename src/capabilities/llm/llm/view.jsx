/**
 * llm 子能力视图(占位)
 *
 * 折叠形态(form 'folded')不渲染右侧 CapabilityPanel,所有交互在 DockedPanel 内。
 * 本组件仅为兼容容器壳的 `view: () => import(...)` 入口而存在;实际不被渲染。
 */
export default function LlmView() {
  return null
}
